import React from 'react';
import { useSelector } from 'react-redux';

const InfoPrDetail = () => {
    // const idPrDetail = useSelector((state) => state.idPrDetail.idPrDetail);

    return (
        <div className="mt-[100px] container">
            <div className="">
                <div className="">{/* <img src={idPrDetail.url} alt="Product" className="" /> */}</div>
            </div>
        </div>
    );
};

export default InfoPrDetail;
